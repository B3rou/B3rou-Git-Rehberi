# 1.0 - Kurulum ve Konfigürasyon (Environment Setup)

> *"Bir marangoz için tezgahının düzeni neyse, bir yazılımcı için terminal ayarları odur. Kör bıçakla ahşap yontulmaz."*

> [!IMPORTANT]
> *Bu modül, Git’e sıfırdan başlayıp konuyu gerçekten anlayarak ilerlemek isteyenler için tasarlanmıştır.* <br>

Git'e başlamadan önce, çalışma ortamımızı sterilize etmemiz gerekiyor. Bu derste "Next > Next > Install" yapıp geçmeyeceğiz; yaptığımız her ayarın arkasındaki **mühendislik sebebini** anlayacağız.

> [!NOTE]
> Ellerimizi kirletmeden önce 2 teorik dersimiz olacak, ardından derslerde birlikte uygulamalı olarak ilerleyeceğiz.

> [!TIP]
> Bu 2 dersin gözünüzü korkutmasına izin vermeyin, zaten okuduğunuzda bitmeyecekler; zamanla pratik yaptıkça bu bilgileri sindireceksiniz. Asla vazgeçmeyin!

---

## 1. CLI vs GUI: Neden "Kara Kutu"?
İnternette *GitKraken, SourceTree* veya *GitHub Desktop* gibi harika görünen grafik arayüzlü (GUI) programlar göreceksin.

> [!CAUTION]
> **YASAK:** Bu eğitim boyunca GUI araçlarını kullanmak kesinlikle yasaktır.

Neden mi?
1.  **Soyutlama:** GUI araçları hataları senden saklar. Terminalde hata aldığında neyin yanlış gittiğini çıplak gözle görürsün.
2.  **Hakimiyet:** Bir sunucuya (Server) bağlandığında elinde mouse veya grafik arayüz olmayacak. Sadece sen ve terminal olacaksınız.
3.  **Hız:** Elini klavyeden çekip mouse'a götürmek, profesyonel bir geliştirici için zaman kaybıdır.

Korkma. Terminal sandığın kadar korkunç değil, sadece biraz **dürüst**.

---

## 2. İşletim Sistemi Farklılıkları ve Kurulum

### Windows Kullanıcıları
Windows dünyasında iki ana yol vardır. İster yalnızca birini kullan, ister ikisini de dene.

> [!NOTE]
> **B3rou'nun Notu:** <br>
> * Bir Windows kullanıcısı olarak ben genellikle **PowerShell** kullanıyorum; çünkü modern ve hızlı. Ancak, bazı durumlarda **Git Bash** kullanmak zorunda kalabilirsin çünkü bazı niş Linux komutlarını PowerShell desteklemez. <br>
> **Tavsiyem:** Her ikisini de kurman. Her yiğidin yoğurt yiyişi farklıdır!

<details>
<summary>🛡️ <b>Seçenek A: Git Bash (Linux Simülasyonu)</b></summary>
<br>
Git Bash, Windows içinde çalışan minik bir Linux terminali gibi davranır. Linux komutlarına aşina olmak istersen bunu kullan.

1. [git-scm.com/download/win](https://git-scm.com/download/win) adresine git.
2. Uygun olan "Standalone Installer"ı indir.
3. Kurulum sırasında **"Git Bash Here"** seçeneğinin işaretli olduğundan emin ol.
</details>

<details>
<summary>⚡ <b>Seçenek B: PowerShell (Modern & Hızlı)</b></summary>
<br>
Git, PowerShell ile de harika çalışır. Git Bash'in eski arayüzü yerine modern bir deneyim sunar.

1. [git-scm.com/download/win](https://git-scm.com/download/win) adresine git.
2. Uygun olan "Standalone Installer"ı indir ve kur.
</details>

> **Alternatif (Power User):** Winget kullanarak tek komutla kurabilirsin:
> ```powershell
> winget install --id Git.Git -e --source winget
> ```

Kurulum bittikten sonra terminalini kapatıp aç ve şu komutu yazarak kontrol et:
```bash
git --version
```

---

### MacOS ve Linux Kullanıcıları

Şanslısın. İşletim sistemin zaten Unix tabanlı olduğu için Git kendi evinde hissedecek.

* **MacOS:** Terminali aç ve `git --version` yaz. Yüklü değilse otomatik pencere açılır veya `brew install git` ile kurabilirsin.
* **Linux:** Çoğu dağıtımda yüklü gelir. Değilse: `sudo apt-get install git` (Ubuntu/Debian) veya `sudo dnf install git` (Fedora).

Ve böylece artık "içeridesin"! Basitçe `git` yazarak komut listesini görebilirsin.

---

## 3. Kritik Detay: Satır Sonları (Line Endings)

Çoğu kursta anlatılmayan ama projeleri patlatan o detay: **Line Endings**. Bu konuyu anlamak zaman gerektirebilir, ancak çok önemli. Bu yüzden en azından önlemimizi şimdiden alalım, gerekirse tekrar tekrar dönüp burayı okuyabilirsin.

Hemen olaya girmeden önce şunu bilmelisin:

> * `CR` (Carriage Return): İmleci satır başına getirir. <br>
> * `LF` (Line Feed): İmleci bir satır aşağı indirir.

Farklı işletim sistemleri satır sonlarını farklı şekilde kodlar:

* **Windows**, satır sonuna `CRLF` (Carriage Return + Line Feed) koyar.

> Yani Windows'ta yeni bir satır başladığında aslında iki karakter eklenir: `\r\n`

* **Mac/Linux**, satır sonuna sadece `LF` (Line Feed) koyar.

> Yani Mac/Linux'ta yeni bir satır başladığında tek karakter eklenir: `\n`

> [!IMPORTANT]
> Sen de fark ettin mi? Aslında sen görmüyor olsan da satır sonlarında görünmez karakterler vardır ve farklıdır!

> [!WARNING]
> Windows'ta her satır sonu aslında iki karakterden oluşuyor. Bu durum, farklı işletim sistemlerinde çalışan ekiplerde ciddi sorunlara yol açabilir.

> [!TIP]
> Örnek olarak eğer Windows kullanan Ömer ile Mac kullanan İbrahim aynı dosyada çalışırsa, Git "Tüm dosya değişti!" sanabilir çünkü görünmez karakterler farklıdır. Bu durum gereksiz **Merge Conflict** (Çakışma) yaratır.

Bunu otomatize etmek için şu ayarı **mutlaka** yap:

**Windows**

```bash
# İndirirken Windows formatını (CRLF) koru, commit atarken Linux formatına (LF) çevir.
git config --global core.autocrlf true
```

**Mac ve Linux**

```bash
# İndirirken dokunma, commit atarken Linux formatına (LF) çevir.
git config --global core.autocrlf input
```

---

## 4. Konfigürasyon: Kilit Ayarlar

Git, yapılan her değişikliğin "kim" tarafından yapıldığını bilmek zorundadır. Bu bilgiye Author bilgisi denir ve tarihe (loglara) derinlemesine kazınır.

### Adım 1: İsim Belirleme

Bu isim loglarda görünür. Gerçek ismini kullanmak zorunda değilsin ama takım arkadaşlarının seni tanıması iyidir.

```bash
git config --global user.name "B3rou"
```

### Adım 2: Mail Belirleme (Kritik!)

Bu mail, GitHub profilinle eşleşmeni sağlar. Eğer yanlış girersen, GitHub commit'lerini senin hesabına bağlayamaz.

```bash
git config --global user.email "mail@b3rou.com"
```

<details>
<summary>🕵️‍♂️ <b>Gizliliğinden Vazgeçemeyenler için: Noreply Email Kullanımı</b></summary>

Eğer kişisel mail adresinin Git loglarında herkese açık görünmesini istemiyorsan, GitHub'ın sağladığı gizli maili kullanabilirsin.

1. GitHub'da <b>Settings > Emails</b> menüsüne git.
2. "Keep my email address private" kutusunu işaretle.
3. Orada verilen <code>id+username@users.noreply.github.com</code> formatındaki maili kopyala.
4. Git ayarına bu maili gir:

```bash
git config --global user.email "123456+B3rou@users.noreply.github.com"
```

</details>

### Adım 3: Varsayılan Dal İsmi (Önemli!)

Eskiden Git; ana çizginize (branch) isim verirken "master" ismini kullanırdı, ancak modern dünyada (ve GitHub'da) standart artık "main" oldu. İleride kafa karışıklığı yaşamamak için bunu şimdiden sabitleyelim:

```bash
git config --global init.defaultBranch main
```

> [!TIP]
> **Yeni Yetenek Kilidi Açıldı: `git config`**
> * `--global`: Ayarların tüm projelerinde geçerli olmasını sağlar.
> * `--list`: Şu ana kadar yaptığın ayarları listeler.

---

## 5. Editör Seçimi

Git, bazen uzun mesajlar yazman için bir editör açar. Varsayılan editör (Vim) bazen çıkışı olmayan bir labirent gibidir. Biz modern bir editör (VS Code) kullanalım.

```bash
git config --global core.editor "code --wait"
```

> [!IMPORTANT]
> **"Command not found" hatası mı aldın?**
> Eğer terminal `code` komutunu tanımıyorsa, VS Code kurulumunda "Add to PATH" seçeneğini işaretlememiş olabilirsin. VS Code'u açıp `Ctrl + Shift + P` yapıp "Shell Command: Install 'code' command in PATH" seçeneğini seçerek düzeltebilirsin. İşletim sisteminin yönergelerini takip ederek PATH'e manuel olarak ekleyebilirsin.

> [!NOTE]
> **Neden `--wait`?**
> Bu komut Git'e şunu söyler: *"Ben editördeki işimi bitirip pencereyi kapatana kadar bekle."* Aksi takdirde Git sabırsız davranıp işlemi boş gönderebilir ve işlem iptal olabilir.

---

## ✅ Kontrol Listesi

Her şey tamam mı? Hadi son bir kontrol yapalım.

```bash
git config --list
```

> [!NOTE]
> **Şu tarz çıktılar görmelisin:**
> ```bash
> user.name=b3rou
> user.email=222520953+B3rou@users.noreply.github.com
> init.defaultbranch=main
> ```

Eğer ismini, mailini ve editörünü görüyorsan hazırsın. 🛠️

👉 **Sonraki Adım:** [1.1 - Git'in Felsefesi](./1.1-git-teorisi.md)